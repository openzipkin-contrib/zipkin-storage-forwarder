# When enabled, this allows shorter env properties (ex -Dspring.profiles.active=kafka-reporter)
zipkin:
  storage:
    kafka-reporter:
      bootstrap-servers: ${KAFKA_REPORTER_BOOTSTRAP_SERVERS:localhost:9092}
      encoding: ${KAFKA_REPORTER_ENCODING:JSON}
      topic: ${KAFKA_REPORTER_TOPIC:zipkin}
